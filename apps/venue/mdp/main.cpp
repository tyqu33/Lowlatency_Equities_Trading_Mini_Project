// apps/venue/mdp/main.cpp
// Entry point for the MDP process (venue market-data publisher).
//
// Skeleton placeholder. TODO: parse config, pin CPU core, attach shm rings, run hot loop.
// No business logic yet.
#include <cstdio>

int main(int /*argc*/, char** /*argv*/) {
    std::puts("[MDP] skeleton process — not implemented yet");
    return 0;
}
