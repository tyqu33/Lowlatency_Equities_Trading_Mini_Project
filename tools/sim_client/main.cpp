// tools/sim_client/main.cpp
// Entry point for the SIM_CLIENT process (background order-flow generator (acts as a participant)).
//
// Skeleton placeholder. TODO: parse config, pin CPU core, attach shm rings, run hot loop.
// No business logic yet.
#include <cstdio>

int main(int /*argc*/, char** /*argv*/) {
    std::puts("[SIM_CLIENT] skeleton process — not implemented yet");
    return 0;
}
